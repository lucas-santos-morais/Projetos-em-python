<h1 align="center"> IMERSÃO ALURA COM PYTHON </h1>

### 📃 Descrição 

Imerção feita pela alura em 2026, prática realizada durante as lives com um projeto final sobre dados com Python. O arquivo em app.py tem um código que extrai dados de um DataFrame e usa esses dados filtrados para expor no dashboard utilizando biblioteca **"streamlit"**. Ter uma conta no streamlit, com ele é possível subir seu projeto.

**OBSERVAÇÃO:**

Os comando usados foram feitos no SO Windows, caso esteja usando outro SO, alguns comandos podem mudar.

### 🖥️ Ambientes virtual

É necessário usar um ambiente virtual **"venv"**

Use o comando: python3 -m venv .venv


### 📚 Bibliotecas utilizadas 

Tem um arquivo **"requimentes.txt"** que precisa rodar antes de iniciar o projeto para instalar as bibliotecas.

Use o comando: pip install -r requirements.txt

* pandas
* streamlit
* plotly




